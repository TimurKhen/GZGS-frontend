export interface StatusBlockInterface {
    icon: string | null, // ВАЖНО ИСПОЛЬЗОВАТЬ ИКОНКИ ТОЛЬКО С https://lucide.dev/icons/
    text: string,
    secondText: string,
    buttonText: string,
    secondButtonText: string,
    isActive: boolean,
}
